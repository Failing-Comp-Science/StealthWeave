# Core Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# Async Task Queue
celery==5.3.6
redis==5.0.1

# Database
psycopg2-binary==2.9.9
sqlalchemy==2.0.25
alembic==1.13.1

# Image Processing
numpy==1.26.3
Pillow==10.2.0
# OpenCV is used ONLY as a fallback video prober in
# modules/capacity/video_capacity._require_cv2 and is imported LAZILY there.
# Never import cv2 at module top-level in the video path: cv2 and PyAV each
# bundle their own FFmpeg (libavdevice), and loading cv2 before PyAV triggers a
# dual-libavdevice objc collision ("Class AVFFrameReceiver is implemented in
# both ...") that can crash the audio/video pipeline. Tested against
# opencv-python 4.9.0.80 (CI) and tolerant of 5.0.x locally.
opencv-python==4.9.0.80
scipy==1.12.0
scikit-image==0.22.0

# Audio Processing
librosa==0.10.1
soundfile==0.12.1
pydub==0.25.1

# Video Processing
imageio==2.33.1
imageio-ffmpeg==0.4.9
# PyAV: bundles a full ffmpeg (with libx264) for frame-level I-frame detection
# and CRF re-encoding in modules/video_stego (audit §7.3). Pinned to a release
# with Python 3.9 wheels; 14.x+ dropped cp39 support. This is the SINGLE
# consistent media-processing stack for the video path (see the OpenCV note
# above). GET /api/healthz/media reports the linked libav* build.
av==13.1.0

# Deep Learning (for VideoSeal and GBRAS-Net)
torch==2.1.2
torchvision==0.16.2

# Cryptography
cryptography==42.0.0

# Error-Correcting Codes (payload ECC layer — Reed-Solomon/BCH)
# Added per audit §7.2 ("Reed-Solomon / BCH ECC layer — none present. Add reedsolo…").
reedsolo==1.7.0

# Metrics and Evaluation
scikit-learn==1.4.0
pandas==2.2.0

# HTTP Client
httpx==0.26.0
aiofiles==23.2.1

# Multipart form parsing (FastAPI file uploads for /api/stego/capacity)
python-multipart==0.0.9

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0

# Development
black==24.1.1
flake8==7.0.0
mypy==1.8.0
